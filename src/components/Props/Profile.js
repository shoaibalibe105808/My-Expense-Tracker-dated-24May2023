



/*
import { getImageUrl } from "./Utils";
function Profile({ profileData }) {
  return (
    <div>
      <h2>
        <b>{profileData.imageAlt}</b>
      </h2>

      <img
        className="avatar"
        src={getImageUrl(profileData.imageId)}
        alt={profileData.imageAlt}
        width={profileData.imageSize}
        height={profileData.imageSize}
      />
      <ul>
        <li>
          <b>Profession: </b>
          {profileData.profession}
        </li>
        <li>
          <b>Awards: {profileData.awards.length} </b>(
          {profileData.awards.join(", ")})
        </li>
        <li>
          <b>Discovered: </b>
          {profileData.discovered}
        </li>
      </ul>
    </div>
  );
}

export default Profile;
*/
